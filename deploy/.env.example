# Sous-domaine public d'OpenWA (doit pointer en DNS A vers l'IP du VPS)
WA_DOMAIN=wa.electroconceptoi.com

# Clé API secrète — le backend l'enverra dans le header X-API-Key.
# Génère-en une forte :  openssl rand -hex 24
API_MASTER_KEY=owa_remplace-moi-par-une-cle-generee
